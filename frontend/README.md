# Mestny Vzglyad Monorepo

Стартовый фронтенд-репозиторий для разработки MVP:
- мобильное приложение (`React Native + Expo`)
- веб-приложение (`React + Vite + TypeScript`)
- общий пакет для переиспользуемых типов и констант

## Выбор стека на текущем этапе

- **Mobile:** Expo (React Native) для максимально быстрого старта на iOS/Android.
- **Web:** React + Vite для быстрой итерации и чистой архитектуры фронтенда.
- **Repo:** монорепозиторий на npm workspaces для общей структуры и синхронной разработки.

## Структура проекта

```
frontend/
  apps/
    mobile/   # Expo-приложение
    web/      # React web-приложение
```

## Требования

- Node.js 20+
- npm 10+
- Xcode/Android Studio (для запуска мобильного приложения на симуляторах/устройствах)

## Первая установка

Из корня `frontend`:

```bash
npm install
```

## Запуск приложений

Из корня `frontend`:

```bash
# web
npm run dev:web

# mobile (Expo)
npm run dev:mobile
```

## Переменные окружения mobile

Файл: `frontend/apps/mobile/.env`

- `EXPO_PUBLIC_API_BASE_URL` — URL backend API (например, `http://localhost:8000/api/v1`)
- `EXPO_PUBLIC_YANDEX_JS_API_KEY` — ключ JS API Яндекс Карт (в текущем MVP карта работает в demo-режиме с карточками POE, даже если ключ не задан)

Важно: в `EXPO_PUBLIC_*` не хранить секреты, эти значения попадают в клиентский бандл.

## Реализованный mobile demo-flow (MVP)

- Регистрация и вход пользователя
- Онбординг предпочтений (интересы, доступность, темп)
- Экран карты/POE с фильтрами, карточками и empty/error состояниями
- Генерация маршрута на основе предпочтений + выбранных POE из карты
- Каталог туров и карточка тура
- Бронирование тура по реальным слотам backend
- Mock-оплата и статусы брони в разделе "Бронирования"
- Отмена брони и mock-refund только для валидных backend-статусов
- Просмотр отзывов тура с backend и отправка отзыва по брони
- Минимальный режим гида (role `employee`/`admin`): мои туры, бронирования по моим турам, базовая статистика

## Что оставлено в backlog после защиты

- Полноценная нативная карта с маркерами (вместо текущего demo-режима карты)
- Полный CRUD кабинета гида (редактирование туров/слотов)
- Production-админка и расширенная аналитика
- Реальная платежная интеграция
